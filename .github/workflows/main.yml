name: github action CI/CD 테스트용
on:
  pull_request:
    branches:
      - develop
      - master
    paths: "frontend/*"

defaults:
  run:
    working-directory: frontend

jobs:
  deployment:
    name: Deployment-action
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          
      - name: Setting node
        uses: actions/setup-node@v3.5.1
        with:
          node-version: 16
          
      - name: npm install & build
        run: |
          npm install
          npm run build
          
      - name: Deploy to github pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./build
          
      - name: Format comment message
        id: format_comment_message
        uses: actions/github-script@v3
        with:
          github-token: ${{secrets.GITHUB_TOKEN}}
          script: |
            const deployURL = 'https://usageness.github.io/gitflow-test/';
            const comments = [
              `## Deploy Preview`,
              `🔥 **check your project preview here!** : [${deployURL}](${deployURL})`
            ].join('\n);
            
            core.setOutput('comment', comment);
            
      - name: comment PR
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          github-token: ${{secrets.GITHUB_TOKEN}}
          message: |
            ${{ steps.format_comment_message.outputs.comments}}
